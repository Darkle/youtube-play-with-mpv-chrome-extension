
settingsStorageKeys = [
  'volume',
  'alwaysOnTop',
  'videoQuality',
  'mpvWindowSize',
  'dontLetYTpageVideoAutoLoad',
  'oscStyle',
  'startMPVpaused'
]

pipe(...fns) -> param -> fns.reduce((result, fn) -> fn(result), param)


export {
  pipe,
  settingsStorageKeys
}
